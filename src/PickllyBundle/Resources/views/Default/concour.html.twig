{% extends 'base.html.twig' %}

{% block header %}
    <div class="container-fluid">
    </div>
{% endblock %}

{% block content %}
    <div class="navmarge">
        <div class="circle">
            <img src="https://x1.xingassets.com/assets/frontend_minified/img/users/nobody_m.original.jpg" alt="..." class="img-circle">
        </div>
        {% for game in games %}
            <div class="cadre-concour">
                <img src="{{ asset('/uploads/images/medias/'~game.image) }}"
                     class="image-concour" alt="">
            </div>
            <div class="cadre2">
                <h2 CLASS="text-cadre">{{ game.title }}</h2>
                <button onClick="javascript:location.href='{{ path('game_index')~game.id }}'" type="button" class="btn btn-warning">Participer</button>
            </div>
        {% endfor %}
    </div>
{% endblock %}

{% block footer %}
    <footer>
        <div class="navbar navbar-inverse navbar-fixed-bottom" >
            <div class="container">
                <div class="navbar-collapse collapse" id="footer-body">
                    <ul class="nav navbar-nav">
                        <li><a class="white" href="{{ path('fos_user_profile_edit') }}">Account Settings</a></li>
                        <li><a class="white" href="{{ path('fos_user_security_logout') }}">Log Out</a></li>
                    </ul>
                </div>
                <div class="navbar-header">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a href ="{{ path('index_home') }}"><i class="fa fa-home" aria-hidden="true"></i><a href>
                </div>

            </div>
        </div>
    </footer>
{% endblock %}